# 本地验证记录

日期：2026-09-08 至 2026-09-09。

## 已验证

- Python 测试：28 个通过。使用真实 Telethon mention 实体与 Baileys 消息解析函数，覆盖提及/回复归属、白名单、去重、历史消息抑制、确认停止、模式切换、失败节流、SQLite 重启恢复、认证及演练隔离。
- Node 测试：4 个通过，覆盖 WhatsApp 手机号/LID mention、媒体说明、临时消息内的引用、回复他人和私聊排除。
- Chromium：HTTP Basic 登录、演练消息、模拟来电调度、全部确认、暂停、四个页面切换均通过；390px 手机视口无横向溢出、无 JavaScript 异常。
- Docker Compose 配置验证通过；Python 与 Node Linux ARM64 镜像实际构建成功，app 健康检查通过。
- Docker 下 `/api/state` 正常，开发运行时的 SQLite 提醒状态保留，WhatsApp `/api/whatsapp/qr` 返回关联二维码。
- 检查输出未包含管理密码、bridge token、二维码内容或第三方密钥。

## 还需要用户账号与设备

- WhatsApp Business 扫码后的实际工作群、真实 @ 和回复接收。
- Telegram 个人账号登录后的群列表、真实 @ 和回复接收。
- CallMeBot 用户授权、免费服务限流、iPhone 锁屏来电和漏接后重拨。
- Pushover 配置、手机使用中提醒、锁屏后的 Apple Watch 触感和重复通知。
- 真实通知链接从 iPhone 打开管理页并确认停止。
- 用户 Ubuntu 服务器上的架构、网络及 HTTPS/VPN 入口。

桌面与手机预览图为演练数据，不代表已连接用户账号或已成功拨通电话。

- 补充验证：一次性测试通道保持当前模式、限流响应包含等待秒数、配置缺失时不会误报成功；确认与在途发送串行，确认完成后不会重拨。

### 面板 Telegram 登录更新

- Python 测试：31 项通过；新增验证码 → 两步验证 → 登录成功、错误验证码、过期、限流、默认不选群、认证/CSRF 及敏感输入不回显检查。
- Chromium：本地容器中的初始登录表单可用；拦截登录接口模拟手机号、验证码、两步验证、成功四步通过；390px 无横向溢出，无 JS 异常。未向 Telegram 发送真实验证码请求，真实账号登录待用户在面板完成。
- 本地 app 容器已重新构建，保留现有配置与数据。

### 群组界面改进

- Python 32 项通过；新增关键词去重、持久化、长度限制，以及隐藏关键词不影响群组监控状态的检查。
- Chromium 使用模拟 API 数据验证 WS/TG 分别计数、平台标签、关键词添加/移除、临时显示、刷新后保留、搜索组合、390px 无横向溢出；职责下拉框和按钮均为 36px，箭头距右边框 12px。无 JavaScript 异常，未修改真实群组或发送通知。

### 连接卡片与控件统一

- 已连接账号显示绿灯并隐藏关联/登录区域；未连接显示红灯及对应入口。关闭二维码后清除图片，忽略迟到的二维码响应。
- Chromium 模拟连接状态验证两种状态切换；桌面 1440px 与手机 390px 检查收件箱、群组、样本、设置四页，所有可见普通按钮、输入框、下拉框高度均为 36px，无横向溢出或 JS 错误。
- Python 32 项通过；本地 app 容器已更新，真实账号与通知未用于交互测试。

### Bark 双通道迁移（取代此前 CallMeBot / Pushover 验证结果）

- Python 34 项通过：Bark 在家 critical / call=1，外出 active 且不含 call，响应判定、正文不外发、演练无网络请求、面板 Key 不回显及即时生效。
- Chromium 模拟 API 检查 Bark Key 保存后清空输入、配置状态更新；桌面与手机四页无溢出，普通控件保持 36px。
- 本地容器已更新。真实 Device Key 和设备权限尚待用户配置；未发送真实 Bark 通知。长响铃不是电话，Watch 触感及锁屏表现需设备实测。

### 无群消息仍重复响铃修复

- 现场查到 8 条待确认提醒全部来自 system/health，连接恢复后旧记录仍被每 120 秒重复发送。
- 已确认并停止这些旧提醒；移除连接异常生成工作提醒的逻辑，启动时取消旧 system/health 待处理记录，调度器排除系统提醒。
- 36 项测试通过，新增仅系统异常不发送、混合队列只发送工作提醒、重启取消遗留系统提醒检查。现场待处理提醒为 0，保留在家模式。

### 设置卡片精简与手动断开

- 顶部两卡片等高并预留登录区域，删除 Bark 安装说明及 DATA 卡片。
- WS/TG 手动断开保留会话与群选择，持久化断开状态；提供重新连接。TG 暂停监听，WA 关闭 socket、停止自动重连及消息处理。
- Python 38 项与 Node 4 项通过；Chromium 模拟接口验证断开/重连、三张卡片、顶部等高和桌面/手机布局。未对真实账号执行断开操作。

### 群组独立配置、草稿保存及批量选择

- WS / TG 分别保存隐藏关键词；升级时复制旧关键词为两份独立配置。
- 当前平台的关键词与勾选通过一次事务保存，验证不存在的群时整体拒绝，不产生部分修改；取消勾选仅在保存后取消该群待处理提醒。
- Python 39 项通过。Chromium 模拟验证独立关键词、排除隐藏后的 tab 数量、底部折叠群列表、批量操作范围、保存前零写入、切换标签保留草稿、隐藏群单独编辑/撤销、保存后刷新保留，以及 1440px / 390px 无溢出。
- 本地 app 已部署，交互验证未修改真实群组。

### 断开连接改为退出账号（取代此前保留会话行为）

- 点击断开先确认，取消不发送请求；接口要求 confirmed=true。确认后调用平台退出、清除本地会话并保持断开，重新连接需要 WhatsApp 扫码 / Telegram 登录。
- Telegram 新建客户端取代已退出客户端；WhatsApp 清除 auth 与旧账号消息暂存，阻止旧连接事件再次写回凭据。
- Python 40 项、Node 4 项通过；覆盖本地会话清除、远端失败后的本地清除、重新登录与确认要求。Chromium 模拟验证取消零写入、确认请求和两平台重新登录入口。未退出真实账号。

### 固定账号卡片高度与菜单连接状态

- 移除 TG 手机号步骤的说明句、使用现有账号说明及登录区顶部分隔线。
- 顶部两张卡片固定 560px；二维码及登录区超出时内部滚动。菜单底部显示 WS/TG 各自状态灯与状态文字。
- Chromium 模拟验证已连接、扫码、手机号、验证码、两步验证在 1440px / 390px 均维持 560px；无横向溢出，菜单连接状态同步更新，取消退出不发请求。HTML 嵌套与 JS 语法检查通过。

### 取消手动测试冷却

- 手动测试不再读取或修改 last_delivery / next_delivery，连续测试不受正式提醒间隔限制。
- 40 项测试通过，包含连续四次测试成功且正式计划、模式、待处理队列不变的检查；未发送真实通知。

### 职责样本版本与云端离线试判

- 新增 sample_reviews、dataset_versions、ai_runs 表；保留既有消息、标签和提醒配置。
- 不可变版本快照、职责草稿、上下文快照、参考/验收用途、默认试判版切换；样本按 WS/TG 每页 50 条。
- 配置接口 Key 不回显，换服务地址须重填；单个任务最多 20 验收样本 × 两个版本，JSON 输出校验、失败/重启中断记录，不自动重试或触发 Bark。
- Python 42 项通过，包括快照不变、配置隔离、验收目标不含金标准标签、用量/结果记录和失败状态；Chromium 模拟验证审核、发布版本、查看快照、密钥清空、试判指标与 390px 无溢出。
- 所有云端调用均使用 mock；未上传真实消息或创建付费训练任务。实际模型微调尚未启用。

### 手机界面独立适配

- 仅 max-width:760px：底部四项导航、安全区留白、16px 输入字体、主要按钮 44–46px、单列表单、卡片随页滚动与更大勾选控件；非提醒页收起重复模式区。
- Chromium 检查 320/375/390/430px 的四个页面无横向溢出，底栏固定于视口底部；样本审核、发布、云端配置/试判使用模拟接口验证。
- 1440px 桌面逐项比较修改前后的主布局、导航、模式卡片、设置卡片及控件的几何尺寸、字号、边距、定位，结果完全一致。检查手机截图确认视觉布局。

### Bark 通知显示来源

- 正式提醒正文按平台和群 ID 汇总群名与条数，最多五个群，超出显示剩余群数；群名截断并移除换行。不发送消息正文。
- 43 项测试通过，覆盖同名跨平台分组、同群合并、长度限制；未主动发送真实测试通知。


## 2026-09-10 审查修复与新增群组提示

- 待确认查询不再受历史 500 条上限影响；全部确认由服务端完成。
- 确认和暂停无需等待 Bark 网络调用；正式发送开始前复核模式和有效待确认项，测试队列独立。
- 间隔和样本保留编辑草稿；关闭页面提醒未保存，发布版本前检查未保存职责与审核。
- 群组设置带 revision，阻止旧设备覆盖；无本地改动时同步新设置。
- 样本请求带序号，拒绝晚到的旧平台结果。
- 完整同步成功后停用已离开的群，保留历史；新增/重新加入的群默认不监控。
- 新群以持久事件返回面板，弹窗显示平台、群名和数量；同一浏览器关闭后不重复弹，同网其他设备可分别查看。提示不经过 Bark，不自动开启监控。
- 退出确认统一说明清除会话、下次重新登录。
- 部署包包含主题曲资源。
- AI 按消息与案例的关键词重合度选参考（最多 6 条，并非向量语义检索/模型微调）；结果列出使用案例。验收可选均匀覆盖或最新样本；对比共用同一批。任务显示进度，允许取消剩余调用，正在进行的调用可能仍计费。
- 成功状态轮询不会抹除业务错误。README 已纠正不存在的 Basic 登录声明，保留当前局域网访问配置。

验证：Python 48 项、WhatsApp Node 4 项通过。浏览器使用完整模拟 API、实际本地静态资源，验证草稿、请求竞态、多设备关键词更新、新群弹窗去重、发布保护、错误保留；四页在 1440 / 390 / 320 px 下无页面级横向溢出、无 JS 运行错误。测试不发送真实通知，不调用云端 AI，不退出真实账号。


## 职责样本表格与本人消息过滤

消息入库保存 from_me；审核列表、统计、草稿版本、旧标注接口及导出排除本人消息，前文仍保留。旧记录身份未知，提供逐条「这是我发的」操作，移出审核但不删除消息、不修改冻结版本。列表改为四列表格，单条展开审核，手机隐藏用途列并在展开表单内编辑。Python 49 项通过，新增回归覆盖本人消息过滤、前文、历史标记及冻结版本保留。


非文字审核过滤：WS/TG 无文字占位消息、空白消息从列表、数量、草稿版本及导出排除；保留带 caption 的消息。旧记录自动适用。前文和 @/回复提醒维持原有行为，历史冻结版本不改写。Python 51 项测试通过。


Bark 分模式配置：旧面板 Key 迁移至外出；旧环境变量仅供外出兼容。在家/外出独立保存、状态及测试，缺少在家 Key 时不得回退到外出。迁移重启不会覆盖已保存的新 Key。Python 52 项通过，模拟浏览器验证两个保存/测试路由及 1440/390/320 px 布局。


新群提示已读状态修复：覆盖此前“每浏览器独立已读”的设计，改为服务器共享游标；升级时现有历史通知一次性标记已读。关闭后其他设备下次刷新同步关闭，后续新群继续提示。弹窗按当前群状态显示已监控/未监控数量，排除已离开的群。54 项 Python 测试和双页面模拟回归通过。


WhatsApp 暂停状态修复：桥接心跳同步 disconnected 标志，状态接口兼容原有分歧，避免实际暂停时仍显示断开按钮。二维码区显示暂停/等待/错误说明，扫码请求去重。55 项 Python 测试及模拟重新连接/二维码显示回归通过；未退出真实账号或清除会话。


群组自动同步与下拉刷新：WhatsApp 连接后自动读取群组，失败 15 秒后重试；连接成功弹出本次群组（显示实际监控状态），应用重启不会把已有群重新当作连接事件。在线每 5 分钟更新 WS/TG，新群使用共享已读提示。移除手动平台刷新按钮。手机页面顶部下拉并松开刷新在线平台及当前内容，保留群组/样本草稿；输入框、内部滚动区及弹窗不触发。57 项后端测试及模拟触摸回归通过。
